const host:string = "http://localhost:3000";

export const apiConfig = {
    apiUrls: {
        single: `${host}/data/single`,
        bulk: `${host}/data/bulk`,
        status: `${host}/status`
    }
}